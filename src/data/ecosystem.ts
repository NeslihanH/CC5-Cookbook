import type { FeatureEntry } from "./types";

// Content Store, Marketplace, ActorCore, Reallusion Hub, plugins, licensing
// tiers. Populated in M5.
export const ecosystemEntries: FeatureEntry[] = [];
